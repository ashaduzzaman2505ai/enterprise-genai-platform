version: "3.9"

services:
  genai-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    restart: unless-stopped
